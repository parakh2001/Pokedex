import 'package:flutter/material.dart';

class Pokeviewbuilder extends StatelessWidget {
  const Pokeviewbuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Container(),
      ),
    );
  }
}
